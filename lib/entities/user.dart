class User {
 int id;
 String username;
 String name;
 String email;
 String password;
 int birthdate;
 String university;
 bool role;


 User(this.id, this.username, this.name, this.email, this.password,
     this.birthdate,
     this.university, this.role);
}